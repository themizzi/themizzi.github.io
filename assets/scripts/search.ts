/**
 * Main search module that imports and registers the custom element
 */

import './search-element.js';

// The custom element is automatically registered when the module is imported
// No need for manual initialization since it's handled by the custom element lifecycle
