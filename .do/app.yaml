name: "personal-site"
static_sites:
- github:
    repo: themizzi/themizzi.github.io
    branch: main
    deploy_on_push: true
  name: site
  source_dir: /
  dockerfile_path: /.devcontainer/Dockerfile
  output_dir: /workspace/public
domains:
- domain: joemizzi.com
  type: PRIMARY
  wildcard: true
